<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../../partials/head', {title: <%= detail.name %>}); %>
    </head>
    <body class="d-flex h-100 text-center text-bg-dark" style="height: 100vh !important">
      <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
        <header class="mb-auto">
          <div>
            <h3 class="float-md-start mb-0"><%= detail.name %></h3>
            <nav class="nav nav-masthead justify-content-center float-md-end">
              <a class="nav-link fw-bold py-1 px-0 active" aria-current="page"
                 href="/ngo/<%= detail.name %>">Home</a>
              <a class="nav-link fw-bold py-1 px-0"
                 href="/ngo/<%= detail.name %>/events">Events</a>
              <a class="nav-link fw-bold py-1 px-0"
                 href="/ngo/<%= detail.name %>/volunteer">Volunteer</a>
            </nav>
          </div>
        </header>

        <main class="px-3">
          <h1><%= detail.name %></h1>
          <p class="lead"><%= detail.description %></p>
          <p class="lead">
            <a class="btn btn-lg btn-light fw-bold border-white bg-white"
               href="/ngo/<%= detail.name %>/volunteer">Volunteer!</a>
          </p>
        </main>

        <footer class="mt-auto text-white-50">
          <p>Created with Auriga.</p>
        </footer>
      </div>
    </body>
</html>
